# 依赖参考；推荐使用 pyproject.toml + uv sync 管理
# 安装: uv pip install -r backend/requirements.txt 或 pip install -r backend/requirements.txt

fastapi>=0.104.0      # Web 框架
uvicorn[standard]>=0.24.0   # ASGI 服务器
python-dotenv>=1.0.0  # 可选，pydantic-settings 已支持 .env
pydantic>=2.5.0      # 数据模型与校验
pydantic-settings>=2.1.0   # 从环境变量加载配置
tomli>=2.0.0          # 解析 Hugo front matter (TOML)
